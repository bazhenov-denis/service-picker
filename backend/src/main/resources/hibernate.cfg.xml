<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Database connection properties -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
<!--        <property name="hibernate.connection.url">${SPRING_DATASOURCE_URL}</property>-->
        <property name="hibernate.connection.url">jdbc:postgresql://db:5432/service_picker</property>
<!--        <property name="hibernate.connection.username">${SPRING_DATASOURCE_USERNAME}</property>-->
<!--        <property name="hibernate.connection.password">${SPRING_DATASOURCE_PASSWORD}</property>-->
        <property name="hibernate.connection.username">service_picker_user</property>
        <property name="hibernate.connection.password">service_picker_dev</property>

        <!-- Other Hibernate properties -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- first time it is create and then onwards update -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Mapping files or annotated classes -->
        <!-- Add your entity classes or mapping files here  for ex: entity used below -->
        <mapping class="com.example.backend.models.Offer"/>
        <mapping class="com.example.backend.models.ProfrolesMapping"/>
        <mapping class="com.example.backend.models.RegionAreaMapping"/>

    </session-factory>
</hibernate-configuration>